node_modules/
dist/
.DS_Store
*.local
.vite/

# Source drop-folders — the served copies live in public/, so we don't
# commit these large originals twice.
MSC Notes and Syllabus/
Resume different domain/
